version: '3.8'
services:
  adserv:
    build: .
    container_name: adserv
    ports:
      - "8000:8000"
    environment:
      - UV_NO_DEV=1
    command: uv run --host

  db:
    container_name: postgres-dev
    ports:
      - "5432:5432"
    env_file:
      - .env
